// Used only by the Playwright browser test — served in place of config.js.
// Values are never used for a real network call (the Supabase CDN import
// itself is intercepted), so these can be anything.
export const SUPABASE_URL = 'https://fake-project.supabase.co';
export const SUPABASE_ANON_KEY = 'fake-anon-key';
